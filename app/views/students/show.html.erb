<h1> Profile Page for <%= @student.first_name %> </h1>

<p> Charities  Raising money for and current amount<p>

<% @student.charities.each do |c| %>
  <div class= "charity_box">
    <%= c.name %>
    <p><%= c.description %></p>
    <p> Goal:</p>
    <p> Amount Pledged:</p>
    <%=  button_to "Pledge", root_path, {method: 'get', class: "pledge_btn" } %>
  </div>
<% end %>
